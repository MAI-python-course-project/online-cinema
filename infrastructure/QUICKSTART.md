# Quick Start Guide - Cinema Platform (Local)

Быстрый запуск всей инфраструктуры локально за 5 минут.

## Предварительные требования

✅ **Docker Desktop** (или Docker Engine + Docker Compose)
   - Минимум 8GB RAM
   - Минимум 20GB свободного места

✅ **Make** (обычно уже установлен на macOS/Linux)

## Запуск за 3 шага

### Шаг 1: Инициализация

```bash
cd infrastructure
make init
```

Эта команда:
- Создаст необходимые директории
- Создаст `.env` файл из `.env.example`
- Подготовит окружение

### Шаг 2: Запуск всех сервисов

```bash
make up
```

Эта команда запустит:
- 📦 **Базы данных**: PostgreSQL, Redis, ClickHouse, Elasticsearch
- 📨 **Message Queue**: Kafka + Zookeeper
- 💾 **Storage**: MinIO (S3-compatible)
- 🌐 **API Gateway**: NGINX + Kong
- 🎬 **Микросервисы**: auth, user, catalog, search, streaming, analytics, payment, notification
- 📊 **Мониторинг**: Prometheus, Grafana, Jaeger
- 📝 **Логирование**: ELK Stack (Elasticsearch, Logstash, Kibana)

**Ожидание**: ~2-3 минуты (первый запуск дольше из-за загрузки образов)

### Шаг 3: Проверка работоспособности

```bash
# Подождите 30-60 секунд, затем:
make health-check
```

## Доступ к сервисам

После успешного запуска доступны:

| Сервис | URL | Логин/Пароль |
|--------|-----|--------------|
| **API Gateway** | http://localhost | - |
| **Kong Admin** | http://localhost:8001 | - |
| **Prometheus** | http://localhost:9090 | - |
| **Grafana** | http://localhost:3000 | admin / admin |
| **Jaeger** | http://localhost:16686 | - |
| **Kibana** | http://localhost:5601 | - |
| **MinIO Console** | http://localhost:9001 | minio / minio_dev_password |

## Проверка API

```bash
# Health check NGINX
curl http://localhost/health

# Health check Kong
curl http://localhost:8001/status

# Health check сервисов (примеры)
curl http://localhost:8001/health  # auth-service
curl http://localhost:8003/health  # catalog-service
```

## Полезные команды

```bash
# Просмотр статуса
make status

# Просмотр логов
make logs

# Просмотр логов конкретного сервиса
make logs-service SERVICE=catalog-service

# Перезапуск
make restart

# Остановка
make down

# Полная очистка (удаление всех данных!)
make clean-volumes
```

## Что делать, если что-то не работает?

### Проблема: Контейнеры не запускаются

```bash
# Проверьте Docker ресурсы
docker system df

# Увеличьте RAM в Docker Desktop:
# Settings → Resources → Advanced → Memory: 8GB

# Перезапустите Docker Desktop
```

### Проблема: Порт уже занят

```bash
# Найдите процесс
lsof -i :8000  # замените на нужный порт

# Остановите процесс или измените порт в .env
```

### Проблема: Out of memory

```bash
# Запустите только инфраструктуру без микросервисов
make infrastructure-up

# Или запустите минимальный набор
cd docker
docker-compose up -d postgres redis kafka catalog-service
```

### Проблема: Сервис не отвечает

```bash
# Проверьте логи сервиса
make logs-service SERVICE=catalog-service

# Перезапустите сервис
cd docker
docker-compose restart catalog-service

# Пересоберите сервис
make rebuild-service SERVICE=catalog-service
```

## Следующие шаги

1. **Разработка сервисов**: Создайте свои микросервисы в `../services/`
2. **Тестирование API**: Используйте Postman или curl для тестирования
3. **Мониторинг**: Откройте Grafana и изучите дашборды
4. **Логи**: Откройте Kibana для просмотра логов

## Дополнительная информация

📖 Полная документация: [README.md](README.md)

💡 Проблемы и вопросы: создайте issue в GitHub

---

**Важно**: Это dev окружение! Для production используйте:
- `docker-compose.prod.yml` или
- Kubernetes манифесты из `k8s/`
